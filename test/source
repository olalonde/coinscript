function main(sig, pubKey) {
  pubKeyHash = 'somehash....';

  pubKeyHashA = hash160(pubKey);

  equal(pubKeyHash, pubKeyHashA);
  //
  assert(equal(pubKeyHash, pubKeyHashA));

  //aa = 'unused variable';

  // should be optimized to OP_CHECKSIGVERIFY?
  //assert(checksig(sig, pubKey));
}
